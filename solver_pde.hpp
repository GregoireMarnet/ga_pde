#ifndef SOLVER_PDE_HPP
#define SOLVER_PDE_HPP

#include <vector>


class solver_pde{

public :

        solver(constant std::vector<double>& a,
                constant std::vector<double>& b,
                constant std::vector<double>& c,
                constant std::vector<double>& d);
    


private:


}

#endif